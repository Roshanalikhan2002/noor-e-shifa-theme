<style>
    :root {
        --primary: #1a3b2b;
        --secondary: #7a1c1c;
        --background: #FAF5EF;
        --white: #FFFFFF;
        --font-serif: 'Playfair Display', serif;
        --font-sans: 'Inter', sans-serif;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: var(--font-sans); background-color: var(--background); color: var(--primary); line-height: 1.6; }

    /* Announcement Bar */
    .announcement-bar {
        background-color: #333;
        color: white;
        text-align: center;
        padding: 8px 0;
        font-size: 13px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    /* Header */
    header {
        background-color: var(--background);
        padding: 20px 5%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        position: sticky;
        top: 0;
        z-index: 100;
        border-bottom: 1px solid rgba(26,59,43,0.1);
    }
    .logo { font-family: var(--font-serif); font-size: 28px; font-weight: 700; text-decoration: none; color: var(--primary); letter-spacing: 2px; }
    nav a { text-decoration: none; color: var(--primary); font-weight: 600; font-size: 14px; margin-right: 25px; text-transform: uppercase; }
    .header-icons { display: flex; gap: 20px; align-items: center; }
    .menu-toggle { display: none; flex-direction: column; gap: 5px; cursor: pointer; }
    .menu-toggle span { display: block; width: 25px; height: 2px; background-color: var(--primary); }

    /* Mobile Drawer */
    .mobile-drawer {
        position: fixed; top: 0; left: -100%; width: 300px; height: 100%;
        background-color: #ffffff; z-index: 2000; transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        padding: 0; display: flex; flex-direction: column;
        box-shadow: 10px 0 40px rgba(0,0,0,0.15);
    }
    .mobile-drawer.active { left: 0; }
    .drawer-banner { background-color: var(--primary); padding: 40px 25px; color: #FFFFFF; }
    .drawer-banner h2 { font-family: var(--font-serif); font-size: 24px; margin: 0; letter-spacing: 1px; }
    .drawer-banner p { font-size: 13px; opacity: 0.8; margin-top: 5px; font-weight: 400; }
    .drawer-nav { padding: 15px 0; flex-grow: 1; overflow-y: auto; display: flex; flex-direction: column; }
    .drawer-link { display: flex; align-items: center; padding: 14px 25px; text-decoration: none; color: #444; font-weight: 600; font-size: 15px; gap: 20px; transition: 0.2s; }
    .drawer-link:hover { background-color: #f9f9f9; color: var(--primary); }
    .drawer-link svg { width: 22px; height: 22px; color: #666; }
    .drawer-divider { height: 1px; background-color: #eee; margin: 10px 0; }
    .drawer-group-label { padding: 20px 25px 10px; font-size: 12px; text-transform: uppercase; color: #999; font-weight: 700; letter-spacing: 1.5px; }
    .drawer-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 1999; display: none; }
    .drawer-overlay.active { display: block; }

    /* About Section */
    .about-hero {
        position: relative;
        height: 60vh;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: white;
    }
    .about-hero img { position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; z-index: -1; filter: brightness(0.7); }
    .about-hero h1 { font-family: var(--font-serif); font-size: clamp(48px, 10vw, 80px); line-height: 1.1; margin-bottom: 20px; }

    .about-content { padding: 100px 5%; max-width: 1200px; margin: 0 auto; }
    .about-section { display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center; margin-bottom: 120px; }
    .about-section:nth-child(even) { direction: rtl; }
    .about-section:nth-child(even) .text-content { direction: ltr; }
    
    .text-content h2 { font-family: var(--font-serif); font-size: 42px; margin-bottom: 30px; line-height: 1.2; }
    .text-content p { font-size: 18px; color: #444; margin-bottom: 20px; }
    
    .image-content { border-radius: 12px; overflow: hidden; height: 500px; box-shadow: 0 20px 50px rgba(0,0,0,0.1); }
    .image-content img { width: 100%; height: 100%; object-fit: cover; }

    .mission-banner {
        background-color: var(--primary);
        color: white;
        padding: 100px 5%;
        text-align: center;
        margin-bottom: 120px;
    }
    .mission-banner h2 { font-family: var(--font-serif); font-size: 48px; margin-bottom: 30px; }
    .mission-banner p { font-size: 20px; max-width: 800px; margin: 0 auto; opacity: 0.9; }

    /* Footer */
    footer { background-color: var(--primary); color: var(--white); padding: 80px 5% 40px; }
    .footer-container { max-width: 1200px; margin: 0 auto; display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr; gap: 40px; }
    .footer-brand h2 { font-family: var(--font-serif); font-size: 28px; margin-bottom: 20px; }
    .footer-brand p { font-size: 14px; opacity: 0.7; line-height: 1.7; }
    .social-links { display: flex; gap: 20px; margin-top: 20px; }
    .footer-column h3 { font-size: 12px; text-transform: uppercase; margin-bottom: 20px; letter-spacing: 1px; }
    .footer-column ul { list-style: none; }
    .footer-column ul li { margin-bottom: 12px; }
    .footer-column ul li a { color: rgba(255,255,255,0.7); text-decoration: none; font-size: 14px; }
    .newsletter-form { display: flex; gap: 10px; margin-top: 20px; }
    .newsletter-form input { background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); padding: 12px; border-radius: 40px; color: white; flex: 1; }
    .newsletter-form button { background: var(--white); color: var(--primary); border: none; padding: 12px 25px; border-radius: 40px; font-weight: 700; cursor: pointer; }
    .footer-bottom { margin-top: 60px; padding-top: 30px; border-top: 1px solid rgba(255,255,255,0.1); text-align: center; font-size: 12px; opacity: 0.5; }

    @media (max-width: 768px) {
        .menu-toggle { display: flex; }
        header nav { display: none; }
        .about-section { grid-template-columns: 1fr; gap: 40px; }
        .about-hero { height: 40vh; }
        .image-content { height: 350px; }
        .footer-container { grid-template-columns: 1fr; }
    }
</style>

<div class="announcement-bar">Free Shipping on Orders Above Rs. 3000</div>

<header>
    <div class="menu-toggle" onclick="toggleMenu()"><span></span><span></span><span></span></div>
    <nav>
        <a href="/">Home</a>
        <a href="/collections/all">Shop</a>
    </nav>
    <a href="/" class="logo">NOOR E SHIFA</a>
    <div class="header-icons">
        <a href="/search" style="color: var(--primary);"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg></a>
        <button onclick="gOpenCart()" style="background:none;border:none;cursor:pointer;color:var(--primary);"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg><span class="g-cart-count">{{ cart.item_count }}</span></button>
    </div>
</header>

<div class="drawer-overlay" id="overlay" onclick="toggleMenu()"></div>
<div class="mobile-drawer" id="mobileDrawer">
    <div class="drawer-banner">
        <h2>NOOR E SHIFA</h2>
        <p>Natural Relief, Purely Crafted</p>
    </div>
    <nav class="drawer-nav">
        <a href="/" class="drawer-link"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path><polyline points="9 22 9 12 15 12 15 22"></polyline></svg>Home</a>
        <a href="/collections/all" class="drawer-link"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>Shop</a>
        <a href="/pages/about" class="drawer-link active"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line></svg>About</a>
        <div class="drawer-divider"></div>
        <div class="drawer-group-label">Communicate</div>
        <a href="/account" class="drawer-link"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>Account</a>
        <a href="/pages/contact" class="drawer-link"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>Contact</a>
    </nav>
</div>

<main>
    <section class="about-hero">
        <img src="{{ 'about_hero.png' | asset_img_url: 'master' }}" alt="Our Story">
        <h1>Our Story</h1>
    </section>

    <div class="about-content">
        <section class="about-section">
            <div class="text-content">
                <h2>The Roots of Tradition</h2>
                <p>Noor e Shifa was born from a deep respect for the healing power of nature. For generations, traditional herbal remedies have been the cornerstone of wellness in our culture.</p>
                <p>We believe that true healing doesn't come from a lab, but from the earth. Our journey started with a simple goal: to share these time-tested secrets with the modern world.</p>
            </div>
            <div class="image-content">
                <img src="{{ 'lifestyle_herbal_1.png' | asset_img_url: '800x' }}" alt="Tradition">
            </div>
        </section>

        <section class="about-section">
            <div class="text-content">
                <h2>Crafted with Intention</h2>
                <p>Every jar of Noor e Shifa Phaki is a result of meticulous sourcing and traditional processing. We only use the purest botanical ingredients, harvested at their peak potency.</p>
                <p>Our unique blend is balanced to work gently with your body, restoring digestive health without the need for harsh chemicals or synthetic additives.</p>
            </div>
            <div class="image-content">
                <img src="{{ 'wake_up_reality.jpg' | asset_img_url: '800x' }}" alt="Crafting">
            </div>
        </section>
    </div>

    <section class="mission-banner">
        <h2>Our Mission</h2>
        <p>To empower everyone to lead a healthier, more balanced life through the wisdom of traditional herbal medicine. We are committed to purity, transparency, and the natural well-being of our community.</p>
    </section>
</main>

<footer>
    <div class="footer-container">
        <div class="footer-brand">
            <h2>NOOR E SHIFA</h2>
            <p>Bringing traditional herbal wisdom to your modern lifestyle. Our mission is to provide natural, safe, and effective digestive solutions for everyone.</p>
        </div>
        <div class="footer-column">
            <h3>Shop</h3>
            <ul>
                <li><a href="/collections/all">All Products</a></li>
                <li><a href="/products/natural-stomach-relief-phaki">Best Sellers</a></li>
            </ul>
        </div>
        <div class="footer-column">
            <h3>Help</h3>
            <ul>
                <li><a href="/pages/contact">Contact Us</a></li>
                <li><a href="#">Shipping Policy</a></li>
                <li><a href="#">Refund Policy</a></li>
            </ul>
        </div>
        <div class="footer-column">
            <h3>Stay Updated</h3>
            <form class="newsletter-form">
                <input type="email" placeholder="Your email">
                <button type="submit">JOIN</button>
            </form>
        </div>
    </div>
    <div class="footer-bottom">&copy; {{ 'now' | date: "%Y" }} Noor e Shifa. All Rights Reserved.</div>
</footer>

<script>
    function toggleMenu() {
        document.getElementById('mobileDrawer').classList.toggle('active');
        document.getElementById('overlay').classList.toggle('active');
    }
</script>
